# Wrangler local state: build cache + account metadata (contains your CF account
# id and the account's email). Never commit — regenerated locally on
# `wrangler dev`/`deploy`.
.wrangler/
node_modules/
dist/
.dev.vars
